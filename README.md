Descargar django y para el controlador de postgresql
  * pip install django 
  * pip install psycopg2
Abrir una nueva terminal en la carpeta y ejecutar el sig:
 * python manage.py runserver
Link para acceder:
 * http://127.0.0.1:8000/inicio/
